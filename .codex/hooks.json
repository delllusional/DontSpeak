{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|shell_command",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); case \"$input\" in *commit*|*merge*) printf '%s' \"$input\" | node \"$(git rev-parse --show-toplevel)/scripts/agents/capture-agent-attribution.mjs\" codex;; esac",
            "commandWindows": "input=$(cat); case \"$input\" in *commit*|*merge*) printf '%s' \"$input\" | node \"$(git rev-parse --show-toplevel)/scripts/agents/capture-agent-attribution.mjs\" codex;; esac",
            "timeout": 30,
            "statusMessage": "Capturing exact commit attribution"
          }
        ]
      }
    ]
  }
}
