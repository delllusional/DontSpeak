/* Empty TU so SwiftPM treats CDontSpeak as a buildable C target whose public
 * header (include/dontspeak.h) is importable from Swift. Symbols come from the
 * linked Rust staticlib libds_core.a. */
