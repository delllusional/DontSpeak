[package]
name = "ds-helper-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "The ds-helper child's stdout reply-token vocabulary — one shared definition for the emitter (ds-helper) and the parser (dontspeakd), so the two sides of the wire contract can't drift."

# Deliberately a zero-dependency leaf: constants only, no formatting/parsing logic.

[lints]
workspace = true
