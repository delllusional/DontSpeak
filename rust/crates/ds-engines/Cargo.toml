[package]
name = "ds-engines"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "Selects and builds DontSpeak's configured STT engine, degrading to an inert placeholder instead of silently substituting another."

[dependencies]
ds-config = { workspace = true }
ds-stt = { workspace = true }
ds-platform = { workspace = true }

[dev-dependencies]
# Tempdir-rooted Paths for stt_box_maps_each_engine, keeping its ClaudeCode arm off the real
# $HOME.
tempfile = { workspace = true }

[lints]
workspace = true
