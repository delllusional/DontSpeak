[package]
name = "ds-tools"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "DontSpeak's single MCP tool catalog, shared by the MCP server and the app FFI so the list never drifts."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
# The typed SetConfigArgs surface (its fields, enums, serde attrs) lives here now (issue #6),
# so the catalog crate depends on ds-config at build time.
ds-config = { workspace = true }
# Chatterbox voice-id validation reads the LIGHT registry mirror (ds-voices depends only
# on ds-config — no ort/rodio/ds-model), keeping this crate a build leaf.
ds-voices = { workspace = true }

[lints]
workspace = true
