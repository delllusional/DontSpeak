[package]
name = "ds-wire"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
description = "DontSpeak's client-wiring orchestrator + writers: the shared home for `wire <client>` (the CLI) and the engine's boot-time reconcile."

[dependencies]
ds-config = { workspace = true }
# hooks.rs::seed_and_prune logs seed success/failure via ds_log::log/LogLevel.
ds-log = { workspace = true }
# io.rs uses serde_json::Value; mcp.rs tests use json!.
serde_json = { workspace = true }

[dev-dependencies]
# The moved writer/orchestrator tests root their Paths at a tempdir.
tempfile = { workspace = true }

[lints]
workspace = true
